<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="p3examtest.Dao.UserDao">
    <resultMap id="userResultMap" type="homework.Do.UserDo">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="gmt_created" property="gmtCreated" />
        <result column="gmt_modified" property="gmtModified" />
    </resultMap>

    <insert id="addUser" parameterType="homework.Do.UserDo" useGeneratedKeys="true" keyProperty="id">
        insert into `user`(username,password,gmt_created,gmt_modified)
        values(#{username},#{password},#{gmtCreated},#{gmtModified})
    </insert>

    <update id="updateUser" keyProperty="homework.Do.UserDo">
        update user
        <set>
            <if test="password != null">
                password = #{password},
            </if>
            gmt_modified= now()
        </set>
        where id=#{id}
    </update>

    <select id="findByUserName" resultMap="userResultMap">
        select * from `user` where username=#{username}
    </select>
</mapper>