<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="p3examtest.Dao.UserDAO">

    <resultMap id="userResultMap" type="p3examtest.Do.UserDO">
        <id column="id" property="id"/>
        <result column="user" property="username"/>
        <result column="password" property="password"/>
        <result column="gmt_created" property="gmtCreated"/>
        <result column="gmt_modified" property="gmtModified"/>
    </resultMap>



    <insert id="add" parameterType="p3examtest.Do.UserDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (user, password,gmt_created,gmt_modified)
        VALUES(#{username}, #{password},now(),now())
    </insert>

    <update id="update" parameterType="p3examtest.Do.UserDO">
        update user
        <set>
            <if test="password != null">
                password=#{password},
            </if>
        </set>
        gmt_modified=now()
        where id=#{id}
    </update>


    <select id="findByUserName" resultMap="userResultMap">
        select * from user where user=#{name}
    </select>



</mapper>